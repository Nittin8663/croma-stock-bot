# Croma Stock Alert Bot

Monitor product stock and get email alerts.

## Setup
1. Configure `email_config.json`
2. Add products to `products.json`
3. Install dependencies: `pip install -r requirements.txt`
4. Run: `python bot.py`
